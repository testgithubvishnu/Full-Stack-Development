// const add = (num1, num2) => num1 + num2; // Implicit conversion
// console.log(add(2, 3));

// When use curly braces it is need to return statement

const add = (num1, num2) => ({ name: "histesh" }); // Implicit conversio
console.log(add(2, 3));
//To return object bind in parentheses
